<h1>Edit <%= @retailer.name %></h1>

<% if @retailer.errors.any? %>
  <div id="error_explanation">
    <h2>Whoops...</h2>
    <ul>
      <% @retailer.errors.full_messages.each do |m| %>
        <li><%= m %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= render partial: 'retailers/form' %>